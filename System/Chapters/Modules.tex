\chapter{Modules}
\section{Module structure}
Modules should always return a table, and if the module has independent game logic, then the module should have an Initialize (labelled "Init") function that runs the module's game logic.
Modules should have all of their requires outside of their Initialize function.